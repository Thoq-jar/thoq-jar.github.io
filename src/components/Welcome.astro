---
import background from '../assets/background.svg';
---

<div id="container">
	<img id="background" src={background.src} alt="" fetchpriority="high" />
	<main>
		<section id="hero">
			<a href="https://github.com/thoq-jar">
				<img src="https://avatars.githubusercontent.com/u/125930852?v=4" width="115" height="auto" alt="pfp" class="pfp" /></a
			>

			<h1>
				Hi! I'm <code><pre>Thoq</pre></code>
			</h1>

			<span class="about-me">
				I know Rust, Gleam, Java and web dev stuff.
				My favorite web framework is probably Flask or Rocket.
				I mainly use Gleam, Elixir or Rust for my projects.
				My favorite colors are Blue, Violet/Purple and Purply-Pink
			</span>

			<section id="links">
				<a class="button" href="https://github.com/thoq-jar">Check out my github</a>
			</section>
		</section>
	</main>
</div>

<style>
	@keyframes rotate {
		0% {
			transform: rotate(0deg);
		}

		50% {
			transform: rotate(180deg);
		}

		100% {
			transform: rotate(360deg);
		}
	}

	body {
		background-color: #000000;
		color: #dddddd;
	}

	.pfp {
		border-radius: 50%;
	}

	#background {
		position: fixed;
		top: 0;
		left: 0;
		width: 100%;
		height: 100%;
		z-index: -1;
		filter: blur(100px);
		animation: rotate linear 60s infinite;
	}

	.about-me {
		width: 60%;
		margin: 1rem auto;
		text-align: center;
	}

	#container {
		font-family: Inter, Roboto, 'Helvetica Neue', 'Arial Nova', 'Nimbus Sans', Arial, sans-serif;
		height: 100vh;
		display: flex;
		justify-content: center;
		align-items: center;
	}

	#links .button {
		padding: 14px 18px;
	}

	#links a.button {
		color: white;
		background: linear-gradient(83.21deg, #8432ff 0%, #9852ee 100%);
		box-shadow: inset 0 0 0 1px rgba(255, 255, 255, 0.13), inset 0 -2px 0 rgba(0, 0, 0, 0.24);
		border-radius: 10px;
		text-decoration: none;
	}

	#links a.button:hover {
		color: rgb(230, 230, 230);
		box-shadow: none;
	}

	main {
		height: 100%;
		display: flex;
		justify-content: center;
		align-items: center;
	}

	#hero {
		display: flex;
		align-items: center;
		flex-direction: column;
		justify-content: center;
		padding: 16px;
		text-align: center;
	}

	h1 {
		font-size: 22px;
		margin-top: 0.25em;
	}

	#links {
		display: flex;
		gap: 16px;
	}

	pre {
		font-family: ui-monospace, 'Cascadia Code', 'Source Code Pro', Menlo, Consolas, 'DejaVu Sans Mono', monospace;
		font-weight: normal;
		background: linear-gradient(14deg, #de98ff 0%, #f7baff 100%);
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
		background-clip: text;
		margin: 0;
	}

	h2 {
		margin: 0 0 1em;
		font-weight: normal;
		color: #cccccc;
		font-size: 20px;
	}

	p {
		color: #b3c9e8;
		font-size: 16px;
		line-height: 24px;
		letter-spacing: -0.006em;
		margin: 0;
	}

	code {
		display: inline-block;
		background: linear-gradient(66.77deg, #3e2a47 0%, #b957e6 100%) padding-box, linear-gradient(155deg, #2f2071 0%, #802fd6 18%, #5e3f6d 45%) border-box;
		border-radius: 8px;
		border: 1px solid transparent;
		padding: 6px 8px;
	}

	@media screen and (max-width: 768px) {
		#container {
			display: flex;
			flex-direction: column;
		}

		#hero {
			display: block;
			padding-top: 10%;
		}

		#links {
			flex-wrap: wrap;
		}

		#links a.button {
			padding: 14px 18px;
		}

		h1 {
			line-height: 1.5;
		}
	}
</style>
